name: liquid_glass_renderer
description: Cross-platform compatibility renderer for liquid_glass_bar.
version: 0.2.0-dev.4
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
