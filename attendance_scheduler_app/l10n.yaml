# Localization config (English primary, Vietnamese — spec §2).
# Run `flutter gen-l10n` to generate AppLocalizations from the ARB files.
arb-dir: lib/l10n
template-arb-file: app_en.arb
output-localization-file: app_localizations.dart
output-class: AppLocalizations
