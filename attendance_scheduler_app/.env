# Flutter runtime config (loaded by flutter_dotenv as a bundled asset).
# Contains no secrets — just where the app calls the backend.
# Points to the local backend started by `make dev` (port 8035).
API_BASE_URL=http://localhost:8035/api/v1
